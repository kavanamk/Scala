val msg= "hello"
val msg2: java.lang.String = "hello again"

def max(x:Int, y: Int): Int =
  if x>y then x
  else y
val bigger = max(3,8)

println("hello world")

def print10(args: String)=
  i=1
  while i<11 do
    println(i)
    i = i+1

In Scala, the foreach method is often used for iterating over collections. 
It is a higher-order function that takes a function as an argument and applies that function to each element in the collection.
Here's an example of using foreach:

 val fruits = List("apple", "banana", "orange")
for(fruit <-fruits){
println(fruit)
}
 fruits.foreach(fruit => println(fruit))

for(i <- 1 to 10){
  println($i)
}

for(i <- 1 to 10
j<- 1 to 5){
  println($i)
}
numbers.foreach(number => println(s"Number: $number"))
